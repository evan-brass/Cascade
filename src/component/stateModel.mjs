"use strict";

import Model from "../model/model.js";

export default Model({
	// TODO: children
	// TODO: Make model expose certain properties as attributes (might be a special function and not inside Model)
}, HTMLElement);